# Runtime dependencies for the bill-forecaster web app (dashboard_app.py).
# Pinned to the versions the frozen v2 artifact was produced and served with.
flask==3.1.2
sentence-transformers==5.5.1
torch==2.12.0
pandas==2.2.3
numpy==2.2.1
pyarrow==22.0.0
gunicorn==23.0.0
