# Session log — 2026-06-11 (session 05, same conversation)

**Participants:** Andy Hall, Claude Code (Fable 5)

## Prompt

Andy: "Go deep on the text. Explore lots of ways to improve the
predictions, and leverage the embeddings to do issue-specific stuff."

## Track A: text-prediction improvements (forecast benchmark, test set)

1. **v2 embeddings — rollcall-level text** (`08_embed_bills.py` rewritten):
   vote_question + vote_desc + strictly pre-vote bill summary. Fixes the
   13% of votes (nominations: 100% vote_desc coverage, nominee + position)
   and amendments that had no/wrong text. Empty-text share: 15.5% -> 0%.
   Encoder held fixed (MiniLM) to isolate the text-construction effect.
   Result: 0.435 -> 0.423 log loss (emb2_tower_mq_8d).
2. **k=16 + MLP projection head**: large discrimination gains (82.0% acc)
   but catastrophic log loss (0.645) — overconfident on future rollcalls.
3. **Diagnosis + fix — temporal internal dev slice**: random in-train-cell
   early-stop/calibration slices are in-sample for rollcall parameters;
   switched to the last 5% of train rollcalls by date (es_mode="temporal";
   still never touches val/test). Result: **emb2_mlp_mq_16d_tcal, test
   log loss 0.349 / 85.2% acc / 84.1% contested / APRE 0.439** — new
   leader by a wide margin; val improved equally (no test-set artifact).
   The fix also improves the linear variant (0.417 -> 0.393).

Forecast benchmark arc today: 0.459 -> 0.411 -> 0.349 log loss;
76.0% -> 85.2% accuracy; contested 69% -> 84%.

## Track B: issue-specific signals

1. **Per-topic ideal points** (`10_issue_positions.py`, VALIDATED):
   23 chamber-topic fits (12 policy areas + Senate nominations), 14,870
   member-topic positions. Topic~overall correlations 0.95-0.99; the
   deviations recover known factions from votes alone: House defense
   leftward deviators = Roy, Ron Paul, Crane, Amash, Massie (libertarian
   non-interventionists); Senate International Affairs leftward = Rand
   Paul, Lee, Hawley; rightward = Menendez, Schumer, Blumenthal (hawkish
   Dems), Romney, Portman. Memo: `Notes/memo_issue_positions.md`.
2. **Embedding probes** (`11_issue_probes.py`, FAILED validation, two
   iterations): v1 (meta-feature phi) badly inverted; v2 (text-only phi +
   contrasts) mixed — guns correct, environment inverted, defense/border
   probes ambiguous (possibly real revealed preference in the
   appropriations era, but indistinguishable from artifact with party
   stereotypes as the only benchmark). Scores written but marked
   exploratory; NOT to be used as member attributes. Path to validation
   documented in the memo (external interest-group ratings, passage-only
   training, stronger encoder).

## Honest-reporting notes

- The probe failure is reported as a failure, with diagnosis; the
  substantive lesson (roll-call "issue positions" = revealed voting on the
  observed agenda, not survey attitudes) is itself a finding.
- All temporal-slice models keep the frozen val/test discipline; the
  internal dev slice is within the train window.

## State

Tests pass. Leaderboard, memos, decision log updated. Still nothing
committed to git (flagged to Andy twice).
