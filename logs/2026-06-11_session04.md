# Session log — 2026-06-11 (session 04, same conversation)

**Participants:** Andy Hall, Claude Code (Fable 5)

## Prompt

Andy: "Do all three" — (1) close the calibration gap, (2) LLM embeddings of
dated summaries, (3) party-loyalty signal extraction.

## (1) Calibrated member-aware tower

Added to `models_texttower.py`: member x question-bucket empirical-rate
logit as a learnable-weight offset (initialized at weight 1), plus post-hoc
temperature+bias calibration (LBFGS on the internal early-stop slice).
Fitted temperature 1.31 on synthetic — confirming the overconfidence
diagnosis. Result: **text_tower_mq_8d, test log loss 0.4105 / 80.1% acc /
75.0% contested / APRE 0.244 — new forecast leader on every metric**
(previous best log loss 0.459).

## (2) Strictly pre-vote LLM embeddings

`08_embed_bills.py`: for each rollcall, the latest CRS summary version with
action_date strictly before the vote date (else title); HTML stripped;
embedded locally with all-MiniLM-L6-v2 (384d). 75% of rollcalls get a
pre-vote summary; nominations have no text. Results: emb_tower_mq_8d test
0.4354 (best contested acc, 75.9%); combining with TF-IDF 0.4331. The
leakage-clean variant costs ~0.024 log loss vs TF-IDF of title/subjects —
treated as an upper bound on subject-assignment leakage in the TF-IDF
variant; both reported, caveat documented in README and decisions.md.

## (3) Party-loyalty residual (first signal memo)

`09_member_signals.py` + `Notes/memo_party_loyalty.md`: loyalty on 21,874
party-unity rollcalls minus the 1D-ideal-point-implied rate, per
member-congress (10,207 cells with n >= 100). Calibrated in aggregate
(91.26% actual vs 91.29% implied); residual SD 3.2pp; near-orthogonal to
extremity (r = -0.16). Validation: recovers Murkowski (-12.0pp), Collins,
Manchin (-7.3), Graham, Fitzpatrick (-7.5) as mavericks; positive tail
(C. Smith +10.1, Massie +8.9) flags members whose 1D position misleads —
the multidimensionality the 8D model exploits. Caveats in memo: in-sample
predictions, agenda endogeneity, residual conflates discipline with
omitted dimensions.

## State

All three threads delivered. Tests pass (19 harness checks + 2 synthetic
model suites). Leaderboards in README regenerate from
Modified Data/results/leaderboard.csv. Nothing committed to git yet.
